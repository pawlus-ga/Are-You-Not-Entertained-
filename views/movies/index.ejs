<h1>
    Search Results
</h1>

<ul>
    <% movies.forEach(movie => { %> 
        <li>
            <a href="/movies/<%= movie.omdbID %>">
                <%= movie.Title %> (<%= movie.Year %>)
            </a>
        </li>
    <% }) %>
</ul>